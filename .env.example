# Infinite Autoclicker - optional environment variables.
# The app needs NO configuration and makes NO network calls. These only tune
# local behavior.

# Force the input *simulation* backend. Default is rdev; set to "enigo" if clicks
# land on the wrong pixel on a scaled multi-monitor Windows setup (SPEC §3).
# IAC_BACKEND=enigo
